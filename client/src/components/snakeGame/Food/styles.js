import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles((theme) => ({
    snakeFood: {
        position: 'absolute',
        width: '2%',
        height: '2%',
        zIndex: 1,
        borderRadius: 3,
    },

}));