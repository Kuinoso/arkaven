import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles((theme) => ({
    wrapper: {
        background: '#AD9D8F',
        width: 'max-content',
        margin: 'auto',
        padding: 5,
        borderRadius: 5,
        marginTop: 10,
    },
    container: {
        display: 'flex',
    },
}));